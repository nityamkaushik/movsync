{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup:libav": "node setup-libav.js",
    "dev": "node setup-libav.js && vite",
    "build": "node setup-libav.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "firebase": "^12.14.0",
    "libav.js": "^6.8.8"
  }
}
